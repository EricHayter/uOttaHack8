# Redis Configuration
# Use 'redis' when running in Docker Compose, 'localhost' when running locally
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
CACHE_TTL=604800  # 7 days in seconds (grocery sales typically last a week)

# Yellow Cake API
YELLOW_CAKE_KEY=your_api_key_here